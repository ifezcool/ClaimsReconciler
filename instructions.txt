Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@db_upload_common.py` around lines 55 - 58, The condition on line 56 that
compares `val == int(val)` will raise OverflowError or ValueError when val is
infinity or NaN, which can occur from Excel formulas. Add a guard using
math.isfinite() to check if the float value is finite before attempting the int
conversion. Update the condition from `if isinstance(val, float) and val ==
int(val):` to also check `math.isfinite(val)` so that infinity and NaN values
skip the integer conversion and are returned as their string representation
instead. Remember to import the math module at the top of the file.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@utils.py` around lines 51 - 56, The lambda function in the apply method for
the "Schedule Number" column has an unsafe condition that raises OverflowError
when x is infinity or an extremely large float value. The check x == int(x)
fails because pd.notna(x) returns True for infinity values. Add a guard
condition before the x == int(x) comparison to check if x is a finite number
using math.isfinite() or np.isfinite(). If x is not finite (infinity or
extremely large), convert it directly to string without attempting the int
conversion to prevent the OverflowError and ensure the reconciliation pipeline
continues processing even when Excel files contain formula errors producing
infinity values.