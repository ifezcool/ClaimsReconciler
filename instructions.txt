Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@compilation_common.py` at line 124, The conditional assignment to df_clean at
line 124 incorrectly falls back to copying the entire dataframe when data_rows
is empty, which causes malformed or totals-only sheets to be treated as valid
claim detail data. Instead of using df.copy() as the fallback when data_rows is
empty, create an empty dataframe or handle the absence of valid detail rows
explicitly to prevent invalid data from being compiled.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@compilation_common.py` around lines 131 - 138, The else branch uses
astype(str) which converts missing values into the literal string "nan", causing
problems in downstream finance comparisons and aggregation. Replace the
astype(str) conversion with the same lambda-based approach used for the special
columns (S_N, BATCH_NUMBER, PROVIDER_CODE) that checks pd.notna(x), converts
non-missing values to strings, and returns empty strings for missing values to
properly preserve NaN as missing rather than as a string literal.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@compilation_common.py` around lines 210 - 215, The code at lines 210-215 has
two issues: the str.contains method with str(sch) over-matches partial schedule
numbers (e.g., schedule 12 matches 112), and Claims_Advised_Amount may be
formatted as strings from Excel causing sum to fail or produce incorrect
results. Replace the str.contains substring matching with exact value matching
by converting the Claim Batch No/Sch No column to numeric type and using
equality comparison with sch, and convert Claims_Advised_Amount to numeric type
using pandas pd.to_numeric with error handling before calling sum to ensure
proper aggregation of amounts.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@compilation_common.py` around lines 242 - 256, The email notification code in
the compilation_common.py section does not validate email addresses before using
them with SMTP. The functions get_to_email() and get_cc_list() return
configuration values that should be validated for proper email format to prevent
SMTP failures. Reuse the recipient email validation logic that was added to
utils.py and apply it to validate the recipient_email returned from
get_to_email() and the addresses in cc_list returned from get_cc_list(). If
validation fails for any address, log an appropriate error and return False
before attempting to construct or send the email message.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@compilation_common.py` around lines 276 - 287, The Schedule_Number and
Source_Files values from uploaded data are being directly interpolated into HTML
without escaping, which creates an HTML injection vulnerability. Import the
html.escape function from Python's standard library at the top of the file, then
apply html.escape() to all instances where Schedule_Number and Source_Files are
inserted into the HTML strings (both in the missing_schedules section and in the
amount_mismatches loop where you build the table rows with f-strings). This will
safely encode any special HTML characters in these user-provided values.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@config.py` around lines 29 - 36, The logging.basicConfig() call in config.py
unconditionally creates a FileHandler that attempts to write to
"claims_reconciler.log" during module import, which can crash the application if
the working directory is not writable. Refactor the logging configuration to
make the FileHandler optional and configurable, while ensuring the StreamHandler
remains as a mandatory fallback. Consider using an environment variable or
configuration flag to control whether file logging is enabled, and only add the
FileHandler to the handlers list if that condition is met.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@db_upload_common.py` around lines 22 - 32, The _convert_date function
currently returns None when a date string doesn't match any supported format,
causing invalid dates to be silently stored as NULL. Instead, raise a ValueError
with a descriptive message after the format-matching loop completes without
success (replacing the final return None statement). This will allow the
existing row-failure reporting mechanism to catch and handle invalid date inputs
properly.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@db_upload_common.py` around lines 169 - 176, The current code commits
successful rows to the database whenever success_count is greater than zero,
even when failed_rows exist, leaving the table partially populated with
incomplete data. To fix this, check whether the failed_rows collection is empty
before deciding to commit or rollback. If failed_rows has any entries, call
conn.rollback() to undo all changes in the batch and display an error message.
Only call conn.commit() when failed_rows is empty, ensuring atomicity—either all
rows are uploaded successfully or none are persisted to maintain data integrity.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@db_upload_common.py` around lines 180 - 182, The cursor.execute call in the
consolidate operation uses INSERT INTO target SELECT * which relies on ordinal
column matching and can corrupt data if columns are reordered or differ between
tables. Replace the SELECT * syntax with explicit column lists that map the
source table columns to the target table columns. Determine the column mapping
between table_name and consolidate_target, then modify the INSERT statement to
explicitly list the target columns after INSERT INTO and the corresponding
source columns in the SELECT clause to ensure correct data flow regardless of
column ordering.

Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@utils.py` around lines 306 - 323, The primary recipient email stored in
recipient_email (from get_to_email()) is not being validated, while the CC list
is validated using validate_email_list(). Add a call to validate_email_list()
for recipient_email after it is retrieved from get_to_email() and before the
message is created in MIMEMultipart(). Pass an appropriate context parameter
like "send_variance_email/to" to identify this as validation for the primary
recipient in the send_variance_email function.